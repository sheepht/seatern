{
  "buildCommand": "npm run build:shared && npm run build:db && npm run build:web",
  "outputDirectory": "packages/web/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
